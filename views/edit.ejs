<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Feature Flag</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="container mt-4">
    <h2>Edit Feature Flag</h2>
    <form action="/flags/update/<%= flag._id %>" method="POST">
        <div class="mb-3">
            <label for="name" class="form-label">Feature Name</label>
            <input type="text" name="name" id="name" class="form-control" value="<%= flag.name %>" required>
        </div>
        <div class="mb-3">
            <label for="isEnabled" class="form-label">Enable Feature</label>
            <select name="isEnabled" id="isEnabled" class="form-control">
                <option value="true" <%= flag.isEnabled ? 'selected' : '' %>>Enabled</option>
                <option value="false" <%= !flag.isEnabled ? 'selected' : '' %>>Disabled</option>
            </select>
        </div>
        <button type="submit" class="btn btn-primary">Update</button>
        <a href="/flags" class="btn btn-secondary">Back</a>
    </form>
</body>
</html>
